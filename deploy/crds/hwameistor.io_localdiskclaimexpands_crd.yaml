apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: localdiskclaimexpands.hwameistor.io
spec:
  group: hwameistor.io
  names:
    kind: LocalDiskClaimExpand
    listKind: LocalDiskClaimExpandList
    plural: localdiskclaimexpands
    singular: localdiskclaimexpand
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LocalDiskClaimExpand is the Schema for the localdiskclaimexpands
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: LocalDiskClaimExpandSpec defines the desired state of LocalDiskClaimExpand
            properties:
              abort:
                default: false
                type: boolean
              claimName:
                description: ClaimName represents which LocalDiskClaim needs to be
                  extended
                type: string
            required:
            - claimName
            type: object
          status:
            description: LocalDiskClaimExpandStatus defines the observed state of
              LocalDiskClaimExpand
            properties:
              expandedDisks:
                description: ExpandedDisks records disks that claimed after creating
                  this expand-job
                items:
                  type: string
                type: array
              lastClaimedDisks:
                description: LastClaimedDisks records disks that claimed before creating
                  this expand-job
                items:
                  type: string
                type: array
              state:
                description: State represents this expand-job state
                enum:
                - Submitted
                - InProgress
                - Failed
                - Completed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
